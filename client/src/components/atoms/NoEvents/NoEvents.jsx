import React from 'react';

import styles from "./style.module.css";

const NoEvents = () => {
  return (
    <div className={styles.noEvents}>
      <h2>There are no events at the present time</h2>
    </div>
  )
}

export default NoEvents;
